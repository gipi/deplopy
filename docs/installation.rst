.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install deplopy

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv deplopy
    $ pip install deplopy
